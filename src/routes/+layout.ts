// Enable static prerendering for the whole site by default.
// Individual routes can opt out with `export const prerender = false`.
export const prerender = true;
export const trailingSlash = 'always';
